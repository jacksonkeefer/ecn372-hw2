# Data and outputs (keep README, train.csv, and OnlineNewsPopularity.names tracked)
OnlineNewsPopularity.csv
data/raw/test.csv

# Trained models (should be regenerated)
Models/*.rds
models/*.rds

# Script used to generate train/test split (reproducible with RANDOM_SEED=42)
split_data.py

# R temporary files
.Rhistory
.RData
.Ruserdata
